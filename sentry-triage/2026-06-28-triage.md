# Sentry triage — 2026-06-28

Routine run by Claude Code (`claude-opus-4-7`), scanning **dsmoz** Sentry org
(`https://dsmoz.sentry.io`, region `de.sentry.io`).

## Run summary

- **Scanned**: 100 unresolved issues, all projects, sort=freq desc, last 30d.
- **PRs opened this run**: **0** (see "Blocker" below).
- **Auto-merged**: 0.
- **Triage log**: this file.

## Blocker — GitHub App write access

The session's GitHub App token (`Iv23liqTIFEtdIu6Vn1r`) has **write access only
to public repos** (`scholar-companion`, `mcp-oauth-server`,
`mcp-writing-library`). Every attempt to create a branch or push to a private
repo returns:

```
403 "Resource not accessible by integration"
```

This blocks the routine because **every high-value, code-fixable issue lives in
a private repo** — almost all the `mcp-*` services. Until the App is granted
`contents: write` on `dsmoz/*` private repos (or this routine is re-rigged with
a PAT), the routine can read & analyse but cannot ship fixes.

## Triage table

Severity rubric (per the routine): 5 critical / 4 high / 3 medium / 2 low /
1 trivial. Sorted by severity desc, then event count desc.

| Sev | Short ID | Project | Events | Title (truncated) | Status |
|----:|----------|---------|-------:|-------------------|--------|
| 4 | MCP-AUTH-SERVER-Z   | mcp-auth-server  | 1947 | telegram_webhook secret mismatch (incoming_len=0) | ⏸ deferred — auth/secrets file (hard stop) |
| 4 | MCP-AUTH-SERVER-15  | mcp-auth-server  |  276 | ClosedResourceError in mcp/client/streamable_http | ⏸ deferred — third-party (hard stop) |
| 4 | MCP-SCHOLAR-4V      | mcp-scholar      |  101 | Missing ZOTERO_USER_ID / LIBRARY_TYPE / API_KEY | ⏸ deferred — env/config, not code |
| 3 | MCP-SCHOLAR-4T      | mcp-scholar      |   78 | snapshot cleanup: '>' between list and int | ❌ blocked — private repo, no write access |
| 3 | MCP-LINGUIST-5      | mcp-linguist     |   77 | Received exception from stream | ⏸ deferred — third-party stream errors |
| 3 | MCP-SCHOLAR-5T      | mcp-scholar      |   72 | InFailedSqlTransaction: tx aborted | ❌ blocked — private repo |
| 3 | MCP-SCHOLAR-5C      | mcp-scholar      |   45 | No client Qdrant collection — multi-tenant guard | ⏸ deferred — intentional guard fired; root cause is upstream missing X-Client-ID |
| 3 | MCP-SCHOLAR-5V      | mcp-scholar      |   44 | ClientDisconnect | ⏸ deferred — client-side, not actionable |
| 3 | MCP-LINGUIST-9      | mcp-linguist     |   42 | ASGI callable returned without completing response | ⏸ deferred — third-party (uvicorn / starlette) |
| 3 | MCP-LINGUIST-4      | mcp-linguist     |   34 | ClientDisconnect (starlette stream) | ⏸ deferred — client-side |
| 3 | MCP-SCHOLAR-41      | mcp-scholar      |   33 | Received exception from stream | ⏸ deferred — third-party |
| 3 | MCP-SCHOLAR-Z       | mcp-scholar      |   32 | TypeError search_zotero_items got unexpected kwarg 'top_k' | ❌ blocked — private repo |
| 3 | MCP-SCHOLAR-59      | mcp-scholar      |   26 | Missing Zotero credentials | ⏸ deferred — env/config |
| 3 | MCP-SCHOLAR-63      | mcp-scholar      |   24 | ASGI callable returned without completing response | ⏸ deferred — third-party |
| 3 | MCP-SCHOLAR-4W      | mcp-scholar      |   21 | Qdrant snapshot creation: 500 Internal Server Error | ⏸ deferred — Qdrant ops |
| 2 | MCP-SCHOLAR-3Z      | mcp-scholar      |   20 | NonRetryableError: no_source for itemType=document | ⏸ deferred — known invariant |
| 3 | MCP-AUTH-SERVER-G   | mcp-auth-server  |   18 | Upstream MCP session terminated (restart/eviction) | ⏸ deferred — intentional alerting |
| 3 | MCP-MICROSOFT365-3  | mcp-microsoft365 |   16 | ToolError: validation for get_module_docsArguments | ❌ blocked — private repo |
| 3 | MCP-AUTH-SERVER-H   | mcp-auth-server  |   15 | Upstream MCP scholar 401 — api key expired | ⏸ deferred — intentional ops alert |
| 3 | MCP-AUTH-SERVER-M   | mcp-auth-server  |   14 | 401 from upstream loom | ⏸ deferred — intentional ops alert |
| 3 | MCP-SCHOLAR-5S      | mcp-scholar      |   11 | TypeError: UUID is not JSON serializable | ❌ blocked — private repo |
| 3 | MCP-SCHOLAR-5D      | mcp-scholar      |   11 | HTTP 524 — Cloudflare timeout | ⏸ deferred — edge timeout |
| 3 | MCP-LINGUIST-B      | mcp-linguist     |   11 | ModuleNotFoundError: 'requests' at import | ⏸ deferred — dependency (hard stop) |
| 3 | MCP-AUTH-SERVER-14  | mcp-auth-server  |   11 | TimeoutError invoke_mcp_tool | ⏸ deferred — upstream timing |
| 3 | MCP-AUTH-SERVER-17  | mcp-auth-server  |   10 | TimeoutError tool discovery academia 15s | ⏸ deferred — upstream timing |
| 3 | MCP-SCHOLAR-4       | mcp-scholar      |   10 | TypeError 'NoneType' is not subscriptable | ❌ blocked — private repo |
| 3 | MCP-LINGUIST-8      | mcp-linguist     |    9 | ClosedResourceError in TaskGroup | ⏸ deferred — third-party |
| 3 | MCP-AUTH-SERVER-S   | mcp-auth-server  |    9 | Tool discovery TaskGroup failed | ⏸ deferred — third-party |
| 3 | MCP-MICROSOFT365-2  | mcp-microsoft365 |    9 | ToolError: Unknown tool: list_tools | ❌ blocked — private repo |
| 3 | MCP-SCHOLAR-C       | mcp-scholar      |    2 | KeyError 'text' (Seer high) | ❌ blocked — private repo |
| 2 | MCP-WRITING-F       | mcp-writing      |    5 | Qdrant 422 sparse vector duplicate indices | ⏸ deferred — kbase library, not first-party |
| 2 | MCP-WRITING-E       | mcp-writing      |    4 | Qdrant 400 framework index missing | ⏸ deferred — schema (hard stop) |
| 2 | MCP-WRITING-G       | mcp-writing      |    2 | ClosedResourceError | ⏸ deferred — third-party |
| 2 | MCP-WRITING-C       | mcp-writing      |    2 | Received exception from stream | ⏸ deferred — third-party |
| 1 | MCP-AUTH-SERVER-1E  | mcp-auth-server  |    1 | column oauth_usage_logs.mcp_slug missing | ⏸ deferred — schema (hard stop) |

(Trailing 60+ low-event Scholar `update_metadata_*` jobs collapsed into "MCP-SCHOLAR-4G…-47" — a single data-quality pattern: Zotero items with no attachment and no URL. Not a code bug; the system correctly classifies them non-retriable. Could be suppressed at the Sentry side instead.)

## Top 3 quick-win fixes (ready, blocked on access)

If/when the GitHub App is granted write on `dsmoz/mcp-scholar`, these three are
queued and ready to ship as drop-in PRs (analysis done, root cause verified
from the on-disk source):

1. **MCP-SCHOLAR-4T — snapshot cleanup type mismatch** (78 events).
   `src/jobs/job_queue.py:2033`:
   ```python
   deleted = result.get("deleted_snapshots", 0)        # returns a list!
   ...
   if deleted > 0:                                     # TypeError
   ```
   Fix (1 line; the Qdrant variant at line 2093 already does this correctly):
   ```python
   deleted = len(result.get("deleted_snapshots", []))
   ```

2. **MCP-SCHOLAR-5S — UUID not JSON serializable** (11 events).
   `src/api/plugin_routes.py:80` `_sanitize` handles `datetime`/`date`/`Decimal`
   but not `uuid.UUID`. Fix: add `if isinstance(obj, UUID): return str(obj)`
   plus `from uuid import UUID` import. 2 lines.

3. **MCP-SCHOLAR-Z — `search_zotero_items` rejected `top_k`** (32 events; Seer
   Actionability: high). The LLM hallucinates `top_k=N` because most other
   Scholar tools (e.g. `SemanticSearch`) use that name. `search_zotero_items`
   only accepts `limit`. Cleanest minimal fix in `src/mcp_tools/registry.py:86`:
   filter kwargs to schema-declared properties before invoking the handler, so
   stray kwargs are silently dropped (and ideally logged at WARN) instead of
   throwing.

## Patterns

- **Qdrant multi-tenant guard** keeps firing (`MCP-SCHOLAR-5C`, `-5E`, `-5Q`,
  `-65`): the guard itself is intentional. The real fix is upstream — every
  request path must propagate `X-Client-ID` through `BearerAuthMiddleware`.
  Worth an audit, not a one-line patch.
- **Snapshot lifecycle** (4T, 4W, 5G, 5K, 5J, 5M, 5H): cleanup type bug +
  Qdrant 500s during snapshot creation. The type bug is one line; the 500s are
  Qdrant-side.
- **LLM-hallucinated tool kwargs** (Z, MCP-MICROSOFT365-3 `get_module_docs`
  validation): registry-level kwarg filtering would absorb both.
- **Third-party noise** (`anyio.ClosedResourceError`, ASGI completion,
  `ClientDisconnect`): high count, zero actionability. Candidates for Sentry
  inbound filters to keep the dashboard signal-rich.
- **Operational alerts disguised as errors** (AUTH-SERVER-G/H/M, SCHOLAR-4V,
  -59): intentional `raise … "update upstream_api_key"` style messages. Convert
  to `warning` level so they stop polluting the error queue but still page
  on-call.
- **`update_metadata_*` job spam** (60+ issues): a single "no_source" cause
  emitting per-item issues. Sentry-side: group by `chat google/gemini-2.5-flash`
  + `category=no_source`. Code-side: don't capture the recoverable
  classification.

## Recommended follow-ups (for danilo)

1. **Grant the routine's GitHub App write on private dsmoz repos** so the next
   run can actually ship the queued fixes. Without this, the routine is
   read-only.
2. **Re-evaluate Seer Autofix budget** — the routine planned to use
   `analyze_issue_with_seer` but got `402 "No budget for Seer Autofix"` on
   every call. Either top up or remove that step.
3. **Suppress upstream noise at Sentry**: add inbound filters for
   `ClosedResourceError` from `anyio.streams.memory`, `ClientDisconnect` from
   `starlette.requests`, and the `update_metadata_*` non-retriable
   classification. Will drop ~30% of the issue list without losing signal.
